package ca.ubc.cs.cpsc210.integerset.util;

/*
 * Represents a set of integers
 */
public class IntegerSet {

    // Inserts element into the set
    // MODIFIES: this
    // EFFECTS: Integer i is added to the IntegerSet if it's
    // not already in the set
    public void insert(Integer i) {
        return;   //stub
    }


    // Removes element from the set
    // REQUIRES: Integer i is in the set
    // MODIFIES: this
    // EFFECTS: Integer i is removed from the IntegerSet
    public void remove(Integer i) {
        return;   //stub
    }

    // Determines if element is in the set
    // EFFECTS: returns true if i is in the set
    public boolean contains(Integer i) {
        return false;  //stub
    }

    // Gets the size of the set
    // EFFECTS: returns number of items in set
    public int size() {
        return 0;     //stub
    }

}
